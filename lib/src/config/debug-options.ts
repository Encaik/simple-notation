import { SNDebugOptions } from '@types';

export default {
  borderbox: {
    info: {
      inner: false,
    },
    score: {
      inner: false,
    },
    stave: {
      inner: false,
    },
    measure: {
      inner: true,
    },
    note: {
      inner: false,
    },
  },
} as SNDebugOptions;
