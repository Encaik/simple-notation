import { SNDebugOptions } from '@types';

export default {
  borderbox: {
    content: {
      inner: false,
      outer: true,
    },
    info: {
      inner: false,
      outer: true,
    },
    score: {
      inner: false,
      outer: true,
    },
    stave: {
      inner: false,
      outer: true,
    },
    measure: {
      inner: false,
      outer: false,
    },
    note: {
      inner: false,
      outer: false,
    },
  },
} as SNDebugOptions;
