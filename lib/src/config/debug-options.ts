import { SNDebugOptions } from '@types';

export default {
  borderbox: {
    content: {
      inner: false,
      outer: false,
    },
    info: {
      inner: false,
      outer: false,
    },
    score: {
      inner: false,
      outer: false,
    },
    stave: {
      inner: false,
      outer: false,
    },
    measure: {
      inner: false,
      outer: false,
    },
    note: {
      inner: false,
      outer: false,
    },
  },
} as SNDebugOptions;
